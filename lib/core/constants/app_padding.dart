import 'package:flutter/material.dart';

class AppPaddings {
  static const defaultPadding = EdgeInsets.fromLTRB(20, 0, 0, 16);
  static const buttonPadding = EdgeInsets.only(right: 16);
  static const buttonContentPadding = EdgeInsets.symmetric(
    vertical: 16,
    horizontal: 32,
  );
}
